import Image from "next/image";

export default function Home() {
  // check if authenticated , with send to route
  // else send to user login
  return (
    <main className="">
      <h2 className="bg-amazon-orange ">hello app</h2>
    </main>
  );
}
